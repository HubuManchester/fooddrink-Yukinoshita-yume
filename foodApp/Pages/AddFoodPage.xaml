<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="foodApp.Pages.AddFoodPage"
             Title="Add Food"
             BackgroundColor="White">
    <ScrollView>
        <VerticalStackLayout Padding="18" Spacing="14">
            <Border StrokeThickness="0" Padding="16" BackgroundColor="#FAF8FA">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <Grid RowDefinitions="Auto,Auto" RowSpacing="4">
                    <Label Text="Add Food Entry"
                           FontSize="26"
                           FontAttributes="Bold"
                           TextColor="#DEB7C1" />
                    <Label Grid.Row="1"
                           Text="Share a food from anywhere in the world."
                           FontSize="14"
                           TextColor="#888" />
                </Grid>
            </Border>

            <Border StrokeThickness="1" Stroke="#E8E0E8" BackgroundColor="White" Padding="14">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="Food Information" FontSize="15" FontAttributes="Bold" TextColor="#C3B9D9" />
                    <Entry x:Name="NameEntry" Placeholder="Food name" />
                    <Entry x:Name="RegionEntry" Placeholder="Region (e.g. Tokyo, Japan)" />
                    <Editor x:Name="DescriptionEditor"
                            Placeholder="Description, ingredients, or story behind the food"
                            AutoSize="TextChanges"
                            HeightRequest="90" />
                </VerticalStackLayout>
            </Border>

            <Border StrokeThickness="1" Stroke="#E8E0E8" BackgroundColor="White" Padding="14">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10" />
                </Border.StrokeShape>
                <VerticalStackLayout Spacing="12">
                    <Label Text="Food Photo" FontSize="15" FontAttributes="Bold" TextColor="#C3B9D9" />
                    <Image x:Name="PreviewImage"
                           HeightRequest="180"
                           Aspect="AspectFit"
                           BackgroundColor="#FAF8FA" />
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Text="Take Photo"
                                BackgroundColor="#DEB7C1"
                                TextColor="#FFFFFF"
                                Clicked="OnTakePhotoClicked" />
                        <Button Grid.Column="1"
                                Text="Choose from Gallery"
                                BackgroundColor="#C3B9D9"
                                TextColor="#FFFFFF"
                                Clicked="OnPickFromGalleryClicked" />
                    </Grid>
                </VerticalStackLayout>
            </Border>

            <Border x:Name="ValidationPanel"
                    IsVisible="False"
                    StrokeThickness="0"
                    BackgroundColor="#FFE8E8"
                    Padding="12">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8" />
                </Border.StrokeShape>
                <Label x:Name="ValidationLabel"
                       TextColor="#C04040"
                       FontAttributes="Bold" />
            </Border>

            <Button Text="Save Entry"
                    FontAttributes="Bold"
                    BackgroundColor="#DEB7C1"
                    TextColor="#FFFFFF"
                    Clicked="OnSaveClicked" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
